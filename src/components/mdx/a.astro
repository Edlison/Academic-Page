---
const props = Astro.props;
const className = [props.class, "text-blue-600 after:content-['_↗']"].filter(Boolean).join(" ");
---

<a {...props} href={props.href} class={className} target="_blank" rel="noopener noreferrer">
  <slot />
</a>
