---
import { MENUS } from "../config";
---

<div class="flex flex-row gap-4 nav-links">
  {
    MENUS.map((item) => (
      <a href={item.link} target={item.target} class="h-full text-sm">
        {item.title}
      </a>
    ))
  }
</div>
